export interface Product {
	title: string;
	testIdSuffix: string;
	price: number;
}

export interface User {
	username: string;
	password: string;
	firstName: string;
	lastName: string;
	postalCode: string;
}
